// Write a JS class that represents a personal record. It has the following properties, all set from the constructor:
//     firstName lastName age email And a method toString(), which prints a summary of the information. See the example for formatting details.
//     Input: The constructor function will receive valid parameters.
//     Output: The toString()method should return a string.
//     Submit the class definition as is, without wrapping it in any function.
// Examples:
//     Sample Input:
//     let person = new Person('Maria', 'Petrova', 22, 'mp@yahoo.com'); console.log(person);
//     Output:
//         Maria Petrova (age: 22, email: mp@yahoo.com)




// Write a JS function that returns an array of Person objects. Use the class from the previous task, create the following instances, and return them in an array:
//     task with js class
// For any empty cells, do not supply a parameter (call the constructor with less parameters).
// As output, return an array with Person instances.
// getPersons(objects) {
// ...
// }
// getPersons([{firstName: 'first', lastName: 'last', age: 0, email: 'foo@bar.com'}]);



// Implement the MyString class, which will have the following methods: the reverse () method, which takes a string as a parameter, and returns it upside down, the ucFirst () method, which takes a string as a parameter, and returns the same string, making its first letter capital and the method ucWords, which takes a string and capitalizes the first letter of each word in that string.
//     Our class should work like this:
// let str = new MyString('abcde f');
// console.log (str); // prints 'abcde f'
// console.log (str.reverse()); // prints 'f edcba'
// console.log (str.ucFirst()); // print 'Abcde f'
// console.log (str.ucWords()); // print 'Abcde F'
